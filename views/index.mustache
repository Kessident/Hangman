<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hangman</title>
    <link rel="stylesheet" href="css/index.css">
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h2>Mystery Word</h2>
        <h3>You have {{guessesRemain}}<br>incorrect guesses left</h3>
      </div>
      <div class="game">
        <div class="word"><p class="chosenWord">{{#chosenWord}}{{#guessed}}{{letter}} {{/guessed}}{{^guessed}}_ {{/guessed}}{{/chosenWord}}</p></div>
        <form action="/" method="post">
          <input class="guessLetter" type="text" name="guess" placeholder="Guess a Letter" autofocus>
          <button type="submit">Guess</button><br><br>
        </form>
        <div class="error"></div>
        <div class="guesses">
          <h3>Guessed Letters</h3>
          <div class="guessedLetters">
          {{#guessedLetters}}
            {{.}},
          {{/guessedLetters}}
        </div>
        </div>
      </div>
    </div>
    <script type="text/javascript" src="js/index.js"></script>
  </body>
</html>
